import { endOfBeat } from 'boombap/helpers/end-of-beat';
import { module, test } from 'qunit';

module('Unit | Helper | end of beat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = endOfBeat([42]);
  assert.ok(result);
});
